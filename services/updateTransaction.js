// services/updateTransaction.js

export const updateTransactionStatus = async (reference, status, reason) => {
    console.log(`Updating transaction ${reference} to status '${status}' with reason: ${reason}`);
    // You can integrate with your database here
  };
  